# Commit current work

Commit current work.

## Instructions

Review each file individually to make sure they're related to the work you just did, then write a brief commit message in the following format:

- Conventional commit prefix without scope, all lowercase (e.g. `feat:`, `fix:`, `chore:`, `refactor:`)
- Short title description, all lowercase (< 80 characters total)
- 2~3 bullet points (< 80 characters) with a quick description

## Notes

- You should only commit work when instructed. Do not keep committing subsequent work unless explicitly told so
- NEVER add a `Co-Authored-By` line to commit messages

Optional: ask me if I would like to push the commit. (Only if I'm not on main)
